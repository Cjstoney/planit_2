import React from 'react';

const Signup = () => {

    // render the logo (component)
    // render the name input box
    
    // render the username (component)
    // render the password (component)
    // button component with props (create new Account)(Already a Member)passed in for each button

}